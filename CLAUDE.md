# AI 開発ガイドライン

## 絶対に守るべき開発ルール

### ルール 1: 無断での機能変更禁止

- ユーザーが実装した機能を許可なく変更・削除・無効化しない
- 既存の動作を変更する前に必ず確認を取る
- ユーザーの実装判断を尊重する

### ルール 2: ハードコード禁止

- 固定値や定数をコードに直接埋め込まない
- 設定可能な値は必ず変数や設定ファイルから読み込む
- 例: データベース値、表示テキスト、API エンドポイント、ファイルパス

### ルール 3: 一時的な対処禁止

- その場しのぎの修正や暫定対応を避ける
- 根本的な解決策を実装する
- TODO や FIXME コメントを残さない

### ルール 4: 作業記録の義務化

- 大きな修正後は必ず memory_bank/にレポートを作成
- フォーマット: yyyy-mm-dd\_機能名.md
- 記録内容: 問題詳細、根本原因、実施した修正、影響範囲、結果

### ルール 5: タスク開始前の確認

- 作業開始前に必ず memory_bank/の実装ログを確認
- **必ず project.md を読んで現在のプロジェクト文脈と要件を理解**
- 関連する過去の修正内容を把握してから開始
- 問題の再発を防ぐ

### ルール 6: 他機能への影響最小化

- 他の機能に影響を与えないよう細心の注意を払う
- 影響範囲を明確に把握してから実装
- 機能を徹底的にテストする

### ルール 7: 大きな変更は事前確認

- 他機能に影響する可能性がある修正は事前にユーザー確認を取る
- 影響範囲と修正内容を明確に説明
- 承認を得てから実装開始

## コード品質基準

### コード再利用原則

- **新しい関数を作成する前に既存関数を確認**
- 類似機能がある場合は再利用またはリファクタリング
- 重複コードの排除を最優先
- 既存のユーティリティ・ヘルパー関数を積極活用

### 関数設計ルール

- 単一責任の原則に従う
- 既存関数で対応可能な場合は新規作成しない
- 再利用性と柔軟性を考慮した設計
- パラメータで動作を制御

## Ultra Thinking 確認チェックリスト

開発中は常に以下の 5 つの観点を確認し、一貫性を保つ:

### 1. プロジェクトの一貫性

- アーキテクチャ方針（PHP メイン）の遵守
- 統一された命名規則とコーディング規約
- ドキュメントと実装の整合性

### 2. 技術選択の妥当性

- 技術選択がプロジェクト目標に適合
- オーバーエンジニアリングの回避
- 将来の保守性を考慮

### 3. セキュリティ考慮事項

- 適切な入力値検証
- 出力時のエスケープ処理漏れなし
- 認証・認可の正しい実装
- セキュリティベストプラクティスの遵守

### 4. パフォーマンスへの影響

- データベースクエリの最適化
- 不要な処理やループなし
- 適切なリソース使用量
- 許容範囲内のレスポンス時間

### 5. X サーバー環境との互換性

- X サーバー制限事項との競合なし
- 適切なパスとディレクトリ構造
- X サーバーでの PHP 設定互換性
- 正しいセッション管理設定

### 基本フロー

1. **PROMPT 生成**

   - Claude がユーザー要件を単一テキストに統合
   - 環境変数$PROMPT に格納


## プロジェクト文脈管理

### プロジェクトドキュメント標準

- **タスク開始前に必ず project.md を読む**
- プロジェクトの仕様、要件、文脈は project.md に記録
- プロジェクトスコープや要件変更時は project.md を更新
- 現在のプロジェクト状態理解のため project.md を参照

### ドキュメント階層

1. **project.md** - プロジェクト仕様、要件、現在の状態
2. **CLAUDE.md** - AI 開発ガイドラインとルール（このファイル）
3. **memory_bank/** - 実装ログと作業レポート

## 管理画面デザインルール

### 必須スキル

管理画面のUI実装時は `/design-principles` スキルを参照すること。
詳細仕様は `spec/design/ui_design_system.md` を参照。

### 決定済みデザイン方針

| 項目 | 決定内容 |
|------|----------|
| デザイン方向 | Utility & Function（GitHub風） |
| カラーテーマ | ライトモード |
| カラー基調 | クールグレー（slate系） |
| 深度戦略 | Borders-only（影控えめ） |
| グリッドシステム | 4pxベース |
| 角丸 | シャープ系（4px, 6px, 8px） |
| フォント | システムフォント |
| アイコン | Bootstrap Icons |
| ナビゲーション | サイドバー形式 |

### CSS変数の使用

- 色は `--color-*` 変数を使用
- スペーシングは `--space-*` 変数を使用
- フォントサイズは `--text-*` 変数を使用
- 角丸は `--radius-*` 変数を使用

### 禁止事項（Anti-Patterns）

- 大きなドロップシャドウ
- 16px以上の角丸（小さい要素）
- 非対称なパディング
- 2px以上の太いボーダー
- 48px以上のマージン
- 装飾的なグラデーション
- 複数のアクセントカラー混在

---

## 実装注意事項

- すべてのルールは例外なく必須
- 作業開始前に必ず project.md を読む
- 重要な変更はユーザー確認を優先
- 包括的なドキュメント維持
- 迅速な解決より長期的な保守性を重視
